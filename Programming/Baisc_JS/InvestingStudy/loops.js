let prices = [100, 200, 300, 400];
let total = 0;

for (let i = 0; i < prices.length; i++) {
    total += prices[i];
}

console.log("Total Amount:", total);

let fiveInput = [5, 10, 5, -2, 0, 7]
let allPositive = true;

for (let i = 0; i > fiveInput.length; i++) {
    if (fiveInput[i] < 0) {
       allPositive = false;
       break
    }
}

switch (day ) {
    case "1":
        console.log("Mondey");
        break;
    case "2":
        console.log("Tuesday");
        break;
    default:
        break;
}